---
id: '223434'
title: 'VSCode 初始化配置'
titleJp: ''
date: '2025.01.04'
excerpt: 'VSCode 初始化配置'
tags: ['vibe-coding']
---


# ✨ 拒绝沉闷！打造 VS Code 极简「奶盖」风：Catppuccin Latte 配置指南

厌倦了千篇一律的黑色编辑器？在这个深色模式统治的世界里，偶尔换上一抹清新的亮色，不仅能缓解视觉疲劳，还能瞬间提升编码时的心情。

今天分享一套我精心调教的 **VS Code 初始化配置**。这套配置以 **Catppuccin Latte (拿铁)** 为基础，配合独家的色彩微调、现代化的字体设置以及丝滑的动画效果，打造出一个既温柔又高效的开发环境。

## 🎨 核心颜值：Catppuccin Latte + 独家微调

众所周知，[Catppuccin](https://github.com/catppuccin/catppuccin) 是目前社区最火的主题之一。而在它的四个变种中，`Latte` 是唯一的亮色主题。

为了避免亮色主题常见的“刺眼”问题，我对配色进行了**低饱和度重构**。

### 1. 必装插件
在开始配置前，请确保安装了以下插件：
*   **主题**：`Catppuccin for VS Code`
*   **图标**：`Material Icon Theme`

### 2. 视觉微调逻辑
默认的 Latte 虽然好看，但对于长时间盯着屏幕的开发者来说，部分颜色对比度略高。通过 `colorOverrides`，我做了一些“降噪”处理：

*   **Lavender (薰衣草色)**：调整为 `#99adff`，一种极柔和的灰蓝色，清爽不抢眼。
*   **Mauve (锦葵紫)**：调整为 `#ce91e8`，降低饱和度，温润优雅。
*   **Text (正文)**：调整为 `#5c5f77`，将纯黑文字调淡，减少黑白对比的生硬感，像铅笔写在纸上的质感。
*   **UI 细节**：行高亮和选区背景都调整为更淡的灰色，让代码像浮在奶油上一样。

## ✒️ 字体与排版：JetBrains Mono

字体是程序员的武器。这里选择了 `JetBrains Mono`，并开启了 **连字 (Ligatures)** 功能。
*   **连字效果**：当你输入 `!=` 时，它会自动变成 `≠`；输入 `=>` 变成 `⇒`。这不仅美观，还能提高代码的可读性。
*   **相对行号**：配置中开启了 `relative` 行号，这是 Vim 党的福音，配合 Vim 插件实现光速跳转。

## 🚀 交互体验：纵享丝滑

高颜值的关键往往在于动态体验。
*   **平滑滚动**：告别生硬的断崖式滚动，让代码浏览像浏览网页一样流畅。
*   **光标动画**：`cursorSmoothCaretAnimation` 让光标移动时带有自然的惯性动画，打字带感。
*   **UI 布局**：将侧边栏（Activity Bar）移至顶部。这是 VS Code 较新的特性，能隐藏笨重的左侧黑条，让界面瞬间清爽，不仅节省水平空间，更像是一个现代化的 IDE。

---

## 🛠️ 抄作业：settings.json 配置代码

按下 `Cmd + Shift + P` (Mac) 或 `Ctrl + Shift + P` (Win)，输入 `Open Settings (JSON)`，将以下内容复制进去即可瞬间变身。

```json
{
  // -------------------------
  // 🎨 主题与外观核心设置
  // -------------------------
  "workbench.colorTheme": "Catppuccin Latte",
  "workbench.iconTheme": "material-icon-theme",

  // -------------------------
  // ✒️ 字体与排版
  // -------------------------
  // 推荐字体：JetBrains Mono (需预先安装)
  "editor.fontFamily": "'JetBrains Mono', Menlo, Monaco, 'Courier New', monospace",
  "editor.fontSize": 14,
  "editor.fontWeight": "400",
  // 开启字体连字（如将 != 显示为 ≠）
  "editor.fontLigatures": true,
  // 相对行号（Vim 玩家必备，跳转飞快，非 Vim 用户可改为 "on"）
  "editor.lineNumbers": "relative",

  // -------------------------
  // 🌊 视觉与动画体验
  // -------------------------
  // 平滑滚动，增加视觉高级感
  "editor.smoothScrolling": true,
  // 光标移动时的平滑动画
  "editor.cursorSmoothCaretAnimation": "on",
  // 光标闪烁方式
  "editor.cursorBlinking": "smooth",

  // -------------------------
  // 🧹 布局极简优化
  // -------------------------
  // 关闭右侧小地图，腾出屏幕空间专注于代码
  "editor.minimap.enabled": false,
  // 将侧边栏图标移到顶部，界面更现代、宽敞
  "workbench.activityBar.location": "top",

  // -------------------------
  // 🖌️ Catppuccin 深度定制 (奶盖风核心)
  // -------------------------
  "catppuccin.colorOverrides": {
    "latte": {
      "lavender": "#99adff", // 极柔和的灰蓝色，清爽不刺眼
      "mauve": "#ce91e8",    // 降低饱和度的淡丁香紫，温润优雅
      "text": "#5c5f77",     // 稍微调淡文字，减少黑白对比的生硬感
      "sky": "#91d7e3"       // 辅助：淡天蓝
    }
  },
  "workbench.colorCustomizations": {
    "[Catppuccin Latte]": {
      "editor.lineHighlightBackground": "#e6e9ef", // 更淡的行高亮，不干扰视线
      "editor.selectionBackground": "#ccd0da"      // 更柔和的选择区域颜色
    }
  },

  // -------------------------
  // 🔌 其他/网络设置
  // -------------------------
  // 自定义插件市场源（如有需要可保留，否则可删除）
  "antigravity.marketplaceGalleryItemURL": "https://marketplace.visualstudio.com/items",
  "antigravity.marketplaceExtensionGalleryServiceURL": "https://marketplace.visualstudio.com/_apis/public/gallery",
  "redhat.telemetry.enabled": true
}
```

## 📸 效果总结

这套配置的核心理念是 **“Soft & Clean”**。

1.  **不累眼**：虽然是亮色，但通过降低文字和高亮背景的对比度，达到了接近电子墨水屏的舒适感。
2.  **现代化**：顶部菜单栏 + 连字字体 + 丝滑动画，让 VS Code 彻底告别“土气”。
3.  **专注**：去掉了 Minimap 和侧边栏的干扰，把最大的屏幕空间留给代码。

如果你也想换个心情，不妨试试这杯“特调拿铁”吧！☕️

---

> **Tips**: 记得去下载 [JetBrains Mono](https://www.jetbrains.com/lp/mono/) 字体并安装，否则字体设置不会生效哦。